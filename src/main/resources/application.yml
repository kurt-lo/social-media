spring:
  application:
    name: social-media
  datasource:
    url: jdbc:mysql://localhost:3306/social_media_db
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect